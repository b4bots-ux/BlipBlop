NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only. Get from https://dashboard.stripe.com/test/apikeys (use the test secret key).
STRIPE_SECRET_KEY=

# Server-only. Get from Supabase project settings -> API -> service_role key.
# Bypasses RLS, so it must never be exposed to the browser or committed.
SUPABASE_SERVICE_ROLE_KEY=
