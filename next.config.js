/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "upload.wikimedia.org",
      "m.media-amazon.com",
      "images.ctfassets.net",
      "images.unsplash.com",
      "bostondynamics.com",
    ],
  },
};

module.exports = nextConfig;